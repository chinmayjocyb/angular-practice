/* import { createAction, props } from '@ngrx/store';
import { IProductList } from './product-data-reducer';

export const update = createAction('Update',  props<{productData:IProductList[]}>());
export const reset = createAction('Reset'); */