// import { createReducer, on } from '@ngrx/store';
// import { update, reset } from './product-data-actions';



/* export const initialState: IProductList[] | [] = [];

export const productDataReducer = createReducer(
  initialState,
  on(update, (state, productData) => {
    console.log(productData, 2334);
    return { ...state, productData };
  }),
  on(reset, () => initialState)
);
*/